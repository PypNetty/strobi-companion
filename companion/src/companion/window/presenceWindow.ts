export const companionWindow = { width: 280, height: 320 }

export const applyPresenceWindow = async (_expanded: boolean) => {
  // Tiny/full is CSS scale only. Resizing the OS window clips the 240px stage
  // out of a transparent frame and makes Strobi vanish.
}
